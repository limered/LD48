name: "godot-ci export"
on: push
env:
  GODOT_VERSION: 4.2.2
  EXPORT_NAME: is-this-a-tiger
  PROJECT_PATH: PutYourHandsDownOnTheGround

jobs:
  export-windows:
    name: Windows Export
    runs-on: windows-latest
    defaults:
      run:
        working-directory: ${{ env.PROJECT_PATH }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true

      - uses: actions/setup-dotnet@v4
        name: 💽 Setup .NET SDK
          
      - name: 📦 Restore Dependencies
        run: |
          dotnet restore

      - name: 🤖 Setup Godot
        uses: chickensoft-games/setup-godot@v2.1.1
        with:
          # Version must include major, minor, and patch, and be >= 4.0.0
          # Pre-release label is optional.
          version: ${{ env.GODOT_VERSION }} # also valid: 4.0.0.rc1 or 4.0.0, etc
          # Use .NET-enabled version of Godot (the default is also true).
          use-dotnet: true
          # Include the Godot Export Templtes (the default is false).
          include-templates: true

      - name: 🔬 Verify Setup
        run: |
          dotnet --version
          "C:\Users\runneradmin\godot\Godot_v4.2.2-stable_mono_win64\Godot_v4.2.2-stable_mono_win64_console.exe" --version

      - name: 🧑‍🔬 Generate .NET Bindings
        run: |
          "C:\Users\runneradmin\godot\Godot_v4.2.2-stable_mono_win64\Godot_v4.2.2-stable_mono_win64_console.exe" --headless --build-solutions --quit || exit 0

      - name: 🦺 Build Projects
        run: |
          dotnet build
          
      - name: Windows Build
        run: |
          New-Item "./builds" -ItemType Directory
          "C:\Users\runneradmin\godot\Godot_v4.2.2-stable_mono_win64\Godot_v4.2.2-stable_mono_win64_console.exe" -v --export-release "Windows Desktop" "builds/${{ env.EXPORT_NAME }}.exe"
          
      - name: Search for Build
        run: tree ./
      
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows
          path: ${{ env.PROJECT_PATH }}\builds\
 